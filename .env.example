DISCORD_BOT_TOKEN=

# OpenAI API key — required for voice STT (Whisper) and TTS
OPENAI_API_KEY=

# Per-user GH token flow (Issue #1)
# Comma-separated Discord user IDs that are treated as admin.
# Admins bypass DM-token flow and use the top-level GH_TOKEN above.
ADMIN_USER_IDS=
